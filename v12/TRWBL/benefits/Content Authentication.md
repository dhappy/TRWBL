Given that users can choose their display name & duplicates are allowed, how do you verify authenticity.

Each user is identified by a set of ENS names. They will be able to link those identities.

The Gold team is the Establishment. Specifically, a Gold membership is your "government name". It could be partitioned according to your county in your state, so *`something`*`.sullivan.county.TN.USA.Gold.Ygg.Army​ 

Each user can attribute provenance by creating links within the overarching tree. Copy-on-write can be used to give users apparent write access to anywhere in the system.

They just need some mechanism for escaping their home directory.

The file system has two components: spots representing coherent concepts & union mounts connecting one spot to many others.

So, when attributing a resource to a creator, they would create a mount originating from the desired spot with a `CONTAINS` edge with a `path` property. The outgoing edges have, optionally, a set of weighted descriptors. 